#ifndef TYPES_HH
#define TYPES_HH

#include <algorithm>
#include <cstdint>
#include <ranges>

using intpos_t   = int16_t;
using uintpin_t  = uint8_t;
using bus_data_t = uint8_t;
using uintimg_t  = uint8_t;
using gameid_t   = uint8_t;

namespace r = std::ranges;

#endif //TYPES_HH
